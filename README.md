# MySejm
An end-to-end data project that fetches monthly voting data from the Polish Parliament, filters and transforms raw records, and loads them into a SQLite database. The processed data is surfaced on a web dashboard, allowing users to easily analyze and inspect parliamentary votes, party alignments, and MP decisions.
